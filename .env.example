# Vite (frontend). Optional: full URL of your deployed Node API (no trailing slash).
# Dev: leave unset — Vite proxies /api to http://localhost:3001 (see vite.config.js).
# Production: set locally or via GitHub Actions secret VITE_API_BASE_URL (see README).
# VITE_API_BASE_URL=https://your-api.fly.dev
